<h3>Bienvenido a <%= title %></h3>
El portal donde podrà crear sus propios juegos!

<table>
	<% var i; for(i=0; i<quizes.lenght; i++) { %>
		<tr>
			<td>
				<a href="quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></a>
			</td>
		</tr>
	<% } %>
</table>
